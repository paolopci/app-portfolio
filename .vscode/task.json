{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Apri IDLE",
            "type": "shell",
            "command": "${config:python.pythonPath}",
            "args": [
                "-m",
                "idlelib"
            ],
            "options": {
                "cwd": "${workspaceFolder}"
            },
            "presentation": {
                "reveal": "always",
                "panel": "dedicated"
            }
        }
    ]
}